// Customize Widget
import 'package:flutter/material.dart';

class CustomizeWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('This is the Customize Step', style: TextStyle(fontSize: 24)),
    );
  }
}
