// Choose Fonts Widget
import 'package:flutter/material.dart';

class ChooseFontsWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'This is the Choose Fonts Step',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
